 <%= render "lists/navbar" %>
 <%= render "lists/banner" %>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit %>
<% end %>

<a class="btn btn-ghost" href=<%=list_path(@list)%>>Back to list</a>

 <%= render "lists/footer" %>
